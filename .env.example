# ═══════════════════════════════════════════════════════════════
#  CRM ALYA VERDENT - Configuração de Ambiente
# ═══════════════════════════════════════════════════════════════
#
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Preencha TODAS as variáveis obrigatórias
# 3. NUNCA commite o arquivo .env no Git
# 4. Rotacione as keys de API regularmente (a cada 90 dias)
#
# ═══════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────
#  BANCO DE DADOS (OBRIGATÓRIO)
# ───────────────────────────────────────────────────────────────
# Formato: mysql://usuario:senha@host:porta/database
# Exemplo Dev: mysql://root:password@localhost:3306/alyacrm
# Exemplo Prod: mysql://user:pass@76.13.81.66:3306/myapp

DATABASE_URL="mysql://usuario:senha@localhost:3306/alyacrm"


# ───────────────────────────────────────────────────────────────
#  AUTENTICAÇÃO JWT (OBRIGATÓRIO)
# ───────────────────────────────────────────────────────────────
# IMPORTANTE: Gere uma string aleatória de 64 caracteres hexadecimais
# Comando para gerar no Node.js:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Comando para gerar no terminal:
#   openssl rand -hex 32

JWT_SECRET="COLOQUE_AQUI_64_CARACTERES_HEXADECIMAIS_ALEATORIOS"


# ───────────────────────────────────────────────────────────────
#  INTELIGÊNCIA ARTIFICIAL - GOOGLE GEMINI (Primário - GRATUITO)
# ───────────────────────────────────────────────────────────────
# Obtenha sua key em: https://makersuite.google.com/app/apikey
# O sistema tenta usar Gemini primeiro (gratuito e rápido)
# Se falhar, usa OpenAI como fallback

GEMINI_API_KEY="AIza_SUA_KEY_AQUI"


# ───────────────────────────────────────────────────────────────
#  INTELIGÊNCIA ARTIFICIAL - OPENAI (Fallback - PAGO)
# ───────────────────────────────────────────────────────────────
# Obtenha sua key em: https://platform.openai.com/api-keys
# Formato: sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
# Limite mensal configurável em USD

OPENAI_API_KEY="sk-proj-SUA_KEY_AQUI"
OPENAI_MONTHLY_LIMIT_USD="5.00"


# ───────────────────────────────────────────────────────────────
#  APLICAÇÃO (OBRIGATÓRIO)
# ───────────────────────────────────────────────────────────────

# Ambiente: "development" ou "production"
NODE_ENV="development"

# Porta do servidor backend
PORT="3001"

# URL do frontend (usado para CORS)
# Desenvolvimento: http://localhost:5173
# Produção: https://alyacrm.com.br
# Múltiplas origens: separar por vírgula (sem espaços)
FRONTEND_URL="http://localhost:5173"


# ═══════════════════════════════════════════════════════════════
#  NOTAS DE SEGURANÇA
# ═══════════════════════════════════════════════════════════════
#
#  ✅ Faça:
#  - Mantenha este arquivo (.env) fora do Git (já está no .gitignore)
#  - Use valores diferentes para desenvolvimento e produção
#  - Rotacione JWT_SECRET a cada 6 meses
#  - Rotacione API keys de IA a cada 90 dias
#  - Use senhas fortes no DATABASE_URL (min 20 caracteres)
#
#  ❌ Nunca:
#  - Commite o arquivo .env no Git
#  - Compartilhe suas keys em mensagens/emails
#  - Use valores padrão em produção
#  - Reutilize JWT_SECRET entre projetos
#
# ═══════════════════════════════════════════════════════════════
